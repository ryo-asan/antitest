# 実験内容と結果のまとめ

## 概要
本プロジェクトでは、精度保証付き数値計算を用いて、特定の偏微分方程式の分岐解（非自明解）の探索および数学的な精度保証（真の解の存在証明）を行いました。
パラメータ $\epsilon$ を変化させた際の解の挙動（分岐図）を詳細に調査するため、パラメータや計算精度を変えながら段階的に実験を実施しました。

---

## 各ディレクトリの実験内容と結果

### 1. `hanpuku/` (初期探索と分岐図のベース構築)
- **目的**: 乱数初期値を用いたニュートン法により、$\epsilon$ ごとに複数の非自明な枝（解）を捉えられるかの初期テスト。
- **設定**: 
  - $\epsilon \in \{0.045, 0.040, 0.035, 0.030\}$ （粗い刻み）
  - 試行回数: 各100回
  - 近似解の次数: 40 から 80 へ拡張
  - MPFR精度: 1500 bits
- **結果**: 
  - 各 $\epsilon$ で2つの大きな枝（振幅の異なる非自明解）を安定して発見し、分岐図の作成パイプラインの構築に成功しました。
  - しかし、この微小な $\epsilon$ の領域では精度保証時の定数（$K, G$）が爆発するため、次数80では判定式を満たせず（`Verification failed`）、真の解の証明には至りませんでした。

### 2. `hanpuku2/` (微細な刻みでの高解像度・分岐図作成)
- **目的**: 分岐図の解像度を上げるため、$\epsilon$ の探索を細かく行う。
- **設定**:
  - $\epsilon \in [0.030, 0.045]$ （0.001刻み、計16ステップ）
  - 試行回数: 各100回（合計1600回の探索）
  - MPFR精度: 1500 bits
- **結果**:
  - 各ステップで代表解を抽出し、点が連続的に繋がる非常に滑らかで高解像度な分岐図（$H^1_0, L^2, L^\infty$ ノルム）の生成に成功しました。
  - 依然としてこの $\epsilon$ 領域では精度保証の条件（$K^2RG \le 0.5$）は満たせませんでした。

### 3. `hanpuku3/` (領域拡張による精度保証の成功とバグ修正)
- **目的**: 定数の爆発が抑えられる、より大きな $\epsilon$ 領域で探索と精度保証を実施。
- **設定**:
  - $\epsilon \in [0.046, 0.060]$ （0.001刻み、計15ステップ）
  - 評価の修正: $L^\infty$ ノルムの評価区間を `[0, 0.5]` から全域 `[0, 1.0]` へ修正し、非対称解のピークを正確に捕捉できるように改善。
  - MPFR精度: 1500 bits
- **結果**:
  - **精度保証に見事成功しました！**
  - $\epsilon \ge 0.057$ の領域では「上側の枝」「下側の枝」の両方で精度保証をクリア。
  - $\epsilon \ge 0.053$ の領域でも少なくとも「上側の枝」の存在が証明されました。
  - 修正した評価区間により、交差・分断のない正しい $L^\infty$ ノルムの分岐図が完成しました。

### 4. `hanpuku4/` (MPFRの超高精度化による検証)
- **目的**: 計算精度を極限まで高めることで、精度保証が及ぶ $\epsilon$ の範囲をさらに広げられるか検証。
- **設定**:
  - $\epsilon \in [0.046, 0.060]$
  - **MPFR精度: 10000 bits** （約3000桁の超高精度へ引き上げ）
- **結果**:
  - **精度保証の成功数は1500 bits時と全く同じ（12個）** でした。
  - これは非常に重要な発見です。精度保証に失敗している原因が、浮動小数点計算の丸め誤差（MPFRのビット数不足）ではなく、関数を有限次元で近似することによる**打ち切り誤差（ルジャンドル基底の次数不足）**にあることが証明されました。
  - 今後さらに小さな $\epsilon$ で精度保証を行うためには、MPFRの精度をこれ以上上げるのではなく、近似解の「次数（現在は80）」を大きく引き上げる（例: 120 や 160）アプローチが必須であることが明確になりました。
